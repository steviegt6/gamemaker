using System;

namespace Tomat.GameMaker.IFF.DataTypes.Models;

[Flags]
public enum GameMakerTrackTraits {
    Unknown1 = 0, // Current speculation that Unknown1 is invisible and Unknown2
    Unknown2 = 1, // is locked. Who knows. :)
}
